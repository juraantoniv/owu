import React from 'react';


const User = ({item}) => {


    return (
        <div>

            <div>{item.id}</div>
            <div>{item.name}</div>


        </div>
    );
};

export default User;

